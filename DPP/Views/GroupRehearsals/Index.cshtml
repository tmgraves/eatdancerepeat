@model IEnumerable<DPP.Models.GroupRehearsal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Day)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Group.GroupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place.Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Day)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Group.GroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Place.Address)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupRehearsalID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupRehearsalID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupRehearsalID })
        </td>
    </tr>
}

</table>
