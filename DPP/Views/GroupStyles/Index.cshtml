@model IEnumerable<DPP.Models.GroupStyle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Group.GroupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Style.Style1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Group.GroupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Style.Style1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupStyleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupStyleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupStyleID })
        </td>
    </tr>
}

</table>
