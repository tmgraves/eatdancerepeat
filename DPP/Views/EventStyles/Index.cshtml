@model IEnumerable<DPP.Models.EventStyle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Event.EventName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Style.Style1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Event.EventName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Style.Style1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventStyleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventStyleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventStyleID })
        </td>
    </tr>
}

</table>
